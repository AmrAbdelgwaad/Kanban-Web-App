/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class",
  content: ["./src/**/*.{html,ts}"],
  theme: {
    extend: {
      colors: {
        // Light
        // Dark
      },
      fontFamily: {
        Jakarta: ["Plus Jakarta Sans", "serif"],
      },
    },
  },
  plugins: [],
};
